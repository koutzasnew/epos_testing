require "spec_helper"

describe Epos::Testing do
  it "has a version number" do
    expect(Epos::Testing::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
